# Main Menu

Front-end entry screen. Title + **Start Game** (loads the Star Map).

First of many sibling screens under `src/ui/screens/`. When a second screen is
added, a `framework/screen_manager` + `shell/` will route between them and this
becomes the first routed screen. Styling comes from `src/ui/theme/`.
